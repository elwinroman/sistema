export default {
    "url_base": "http://localhost/github/sistema/" 
}

// inicializacion
// this.root = document.querySelector(':root');
// this.rs = window.getComputedStyle(this.root);

// this.sidebarWidth = {       // css root values
//     default : '250px',
//     short   : '80px'
// };
// this.root.style.setProperty('--sidebar-width', this.sidebarWidth.default);